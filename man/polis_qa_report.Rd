% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qa_report.R
\name{polis_qa_report}
\alias{polis_qa_report}
\title{Build a unified QA report from a preprocessing output folder}
\usage{
polis_qa_report(polis_folder, registry = polis_qa_checks(), verbose = TRUE)
}
\arguments{
\item{polis_folder}{Path to the folder the preprocessing run wrote to.}

\item{registry}{QA check registry (default \code{\link[=polis_qa_checks]{polis_qa_checks()}}); a tibble
mapping filename patterns to a check name, domain, severity and
description.}

\item{verbose}{Emit a cli report (default \code{TRUE}).}
}
\value{
A list with:
\describe{
\item{\code{summary}}{Tibble: one row per check found, with \code{check},
\code{domain}, \code{severity}, \code{n_flagged} (rows), \code{n_files}, \code{description}.}
\item{\code{files}}{Tibble of every matched file and its row count.}
\item{\code{meta}}{Totals and the folder scanned.}
}
}
\description{
Scans \code{polis_folder} (recursively) for the data-quality side files written
by a POLIS preprocessing run, counts the rows each check flagged, and returns
a tidy summary. Unknown files are ignored; missing checks simply don't appear.
}
\examples{
\dontrun{
qa <- polis_qa_report("data/polis")
qa$summary
}
}
