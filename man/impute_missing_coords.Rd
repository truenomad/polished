% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{impute_missing_coords}
\alias{impute_missing_coords}
\title{Place a random point inside the district polygon for cases missing coordinates}
\usage{
impute_missing_coords(
  data,
  shape_adm2,
  guid_var = "adm2_guid",
  lon_var = "longitude",
  lat_var = "latitude",
  shape_guid_var = "adm2_guid",
  seed = 1234,
  fallback_buffer = 3000,
  verbose = TRUE
)
}
\arguments{
\item{data}{A case data frame carrying an ADM2 GUID and coordinate columns.}

\item{shape_adm2}{An \code{sf} object of district polygons with an \code{adm2_guid}
column (the \code{spatial_global_adm2} layer from \code{\link[=process_spatial]{process_spatial()}}).}

\item{guid_var}{Case ADM2 GUID column. Default \code{"adm2_guid"}.}

\item{lon_var, lat_var}{Case coordinate columns. Default \code{"longitude"} /
\code{"latitude"}.}

\item{shape_guid_var}{ADM2 GUID column in \code{shape_adm2}. Default
\code{"adm2_guid"}.}

\item{seed}{Integer seed for reproducible sampling. Default \code{1234}.}

\item{fallback_buffer}{Buffer radius in metres for the centroid fallback when
a polygon cannot be sampled. Default \code{3000}.}

\item{verbose}{Emit a cli summary. Default \code{TRUE}.}
}
\value{
\code{data} with \code{lon_var}/\code{lat_var} filled for sampled rows and a logical
\code{coord_imputed} column marking them.
}
\description{
For each case whose coordinates are missing or \verb{(0, 0)}, draws a random point
uniformly within its district (\code{adm2}) polygon, so downstream maps and
point-in-polygon work have a usable location. Cases with valid coordinates are
untouched. A district whose polygon cannot be sampled falls back to its
centroid buffered by \code{fallback_buffer} metres.
}
\examples{
\dontrun{
shp <- qs2::qs_read("spatial_global_adm2.qs2")
impute_missing_coords(cases, shp)
}

}
