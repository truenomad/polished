% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_afp.R
\name{enrich_afp}
\alias{enrich_afp}
\title{Enrich cleaned AFP data with country groupings and AFP flags}
\usage{
enrich_afp(
  data,
  iso3_var = "country_iso3code",
  class_var = "classification",
  virus_var = "polio_virus_types",
  surv_var = "surveillance_type_name",
  adm0_var = "adm0",
  type_var = "classification_all",
  lookup = NULL
)
}
\arguments{
\item{data}{A cleaned AFP data frame (e.g. the output of \code{\link[=clean_afp]{clean_afp()}}).}

\item{iso3_var}{Country ISO3 column. Default \code{"country_iso3code"}.}

\item{class_var}{Raw classification column. Default \code{"classification"}.}

\item{virus_var}{Poliovirus-types column. Default \code{"polio_virus_types"}.}

\item{surv_var}{Surveillance-type column. Default \code{"surveillance_type_name"}.}

\item{adm0_var}{Admin-0 name column, used as the \code{country_actual} fallback.
Default \code{"adm0"}.}

\item{type_var}{Column the serotype is read from for \code{polio_type} (default
\code{"classification_all"}, falling back to \code{virus_var}).}

\item{lookup}{Optional country lookup table (same columns as
\code{\link[=polis_country_lookup]{polis_country_lookup()}}); the packaged lookup is used when \code{NULL}.}
}
\value{
\code{data} with \code{country_actual}, \code{risk_group}, \code{epi_zones},
\code{epi_zones_v2}, \code{polio_type}, \code{afp_class}, \code{afp}, \code{npafp} and
\code{pending_results} added where their inputs are present.
}
\description{
An optional layer over \code{\link[=clean_afp]{clean_afp()}} that adds, where derivable:
\itemize{
\item country-keyed reference attributes from \code{\link[=polis_country_lookup]{polis_country_lookup()}} --
\code{country_actual} (standardised name, falling back to the title-cased
admin-0 name), \code{risk_group} (Endemic / Very High / High / Medium Risk),
and \code{epi_zones} / \code{epi_zones_v2};
\item \code{polio_type} (Type 1 / 2 / 3) read off the serotype in the
classification;
\item the surveillance AFP flags \code{afp_class} (AFP-Positive / Non-polio AFP /
Not an AFP / VAPP / Others), the binary \code{afp} and \code{npafp}, and
\code{pending_results}.
}
}
\examples{
enrich_afp(data.frame(
  country_iso3code = c("NGA", "PAK"),
  classification = c("Discarded", "Confirmed (wild)"),
  classification_all = c("NPAFP", "WPV 1"),
  polio_virus_types = c(NA, "WILD1"),
  surveillance_type_name = c("AFP", "AFP")
))

}
