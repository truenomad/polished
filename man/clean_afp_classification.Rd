% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_afp.R
\name{clean_afp_classification}
\alias{clean_afp_classification}
\title{Derive the fused AFP virus type and analytic classification}
\usage{
clean_afp_classification(data)
}
\arguments{
\item{data}{A cleaned AFP data frame carrying at least \code{classification}
(and ideally \code{polio_virus_types}, \code{vdpv_classifications}).}
}
\value{
\code{data} with \code{vtype}, \code{vtype_fixed}, \code{classification_all},
\code{sabin1}/\code{sabin2}/\code{sabin3} and (when derivable) \code{hot_case} added; the raw
\code{classification}, \code{polio_virus_types} and \code{vdpv_classifications} columns are
left untouched.
}
\description{
Decodes the specific poliovirus and fuses it with the case classification
into one analytic label, using standard \strong{WPV} (wild poliovirus)
nomenclature throughout -- \emph{not} the legacy \verb{WILD n} strings, which do not
match how downstream surveillance code filters (\code{grepl("WPV|cVDPV", ...)}).
}
\details{
Two layers:
\enumerate{
\item \code{vtype} / \code{vtype_fixed} decode the virus from \code{polio_virus_types} +
\code{vdpv_classifications}. A VDPV always carries an explicit kind prefix --
\code{cVDPV} (circulating), \code{aVDPV} (ambiguous), \code{iVDPV} (immune-deficient) --
so the three are never merged or silently dropped; an untyped \verb{VDPV n}
only remains when the kind is genuinely unknown. A few historical country
corrections patch early records (Congo 2010, Nigeria 2011, pre-2010 wild)
where the virus field was not yet populated.
\item \code{classification_all} is the single analysis label: the \code{vtype_fixed} virus
string for virus-positive cases, otherwise the raw POLIS \code{classification}
recoded -- Discarded -> NPAFP, Compatible -> COMPATIBLE, Not an
AFP -> NOT-AFP, Pending -> PENDING (LAB PENDING when the specimen never
reached the lab), VAPP -> VAPP, Not Applicable/Others/VDPV -> UNKNOWN.
Cases matching none stay \code{none}/\code{NA} for manual review.
}
}
\section{Classification vocabulary (match on these prefixes, not free text)}{

\describe{
\item{Wild}{\verb{WPV 1}, \verb{WPV 2}, \verb{WPV 3}, \code{WPV1andWPV3} -- prefix \code{WPV}.}
\item{Circulating VDPV}{\verb{cVDPV 1/2/3} and combinations -- prefix \code{cVDPV}.}
\item{Ambiguous VDPV}{\verb{aVDPV 1/2/3} -- prefix \code{aVDPV}. \strong{Include/exclude is
a deliberate analyst choice}; these are labelled, never folded into
\code{cVDPV}.}
\item{Immune-deficient VDPV}{\verb{iVDPV 1/2/3} -- prefix \code{iVDPV}. Same explicit
choice as \code{aVDPV}.}
\item{Untyped VDPV}{\verb{VDPV 1/2/3} -- a VDPV whose kind is unknown.}
\item{Wild + VDPV co-detection}{\code{WPV1and...} (e.g. \verb{WPV1andcVDPV 2}).}
\item{Non-virus}{\code{NPAFP}, \code{COMPATIBLE}, \code{NOT-AFP}, \code{PENDING},
\verb{LAB PENDING}, \code{VAPP}, \code{UNKNOWN}.}
}
So "any WPV1" is \code{grepl("^WPV 1|^WPV1and", classification_all)}, and
"any circulating VDPV2" is \code{grepl("cVDPV 2", classification_all)}.

Also derives the Sabin-detection flags (\code{sabin1}/\code{sabin2}/\code{sabin3}) and,
where the paralysis fields are present, a recomputed \code{hot_case} (POLIS also
ships \code{paralysis_hot_case}; this applies the standard
asymmetric + onset-fever + rapid-progression definition, which can differ).
Every condition is NA-safe: a missing classification/admin/year leaves the
prior value intact rather than nulling it.
}

\examples{
clean_afp_classification(data.frame(
  classification = c("Discarded", "Confirmed (wild)"),
  polio_virus_types = c(NA, "WILD1"),
  vdpv_classifications = c(NA, NA)
))

}
