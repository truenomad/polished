% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{load_polis}
\alias{load_polis}
\title{Load a year/country slice across all processed datasets}
\usage{
load_polis(
  project,
  year = NULL,
  country = NULL,
  datasets = NULL,
  as = c("tibble", "dtplyr", "dt", "arrow")
)
}
\arguments{
\item{project}{A \link{polis_project}.}

\item{year}{Optional year (or years) to keep. Matched against the first of
\code{year}, \code{year_onset}, \code{year_start}, \code{collect_yr} present in each table.}

\item{country}{Optional country/ISO3 value(s) to keep. Matched against the
first of \code{iso3}, \code{country_iso3code}, \code{adm0}, \code{country} present.}

\item{datasets}{Optional character vector restricting which datasets to load
(by folder name under \verb{processed/}). Default \code{NULL} (all present).}

\item{as}{Shape of each returned element: \code{"tibble"} (collected, default),
\code{"dtplyr"} (a mutable \code{lazy_dt} for data.table-backed downstream work),
\code{"dt"} (a \code{data.table}), or \code{"arrow"} (the lazy, uncollected query).}
}
\value{
A named list of tables (one per dataset), each filtered to the slice.
}
\description{
Opens every dataset written to the project's \verb{processed/} zone, pushes a
\code{year} and/or \code{country} filter down to the parquet scan, and returns a named
list with one filtered table per dataset (\code{afp}, \code{es}, \code{sia}, \code{virus}, and any
others present, such as \code{indicators}). Because \verb{processed/} is partitioned by
year, a year filter prunes whole partitions; the country filter uses parquet
predicate pushdown. Only the matching slice is materialised.
}
\examples{
\dontrun{
proj <- init_polis_project(file.path(tempdir(), "demo"), quiet = TRUE)
slice_2025 <- load_polis(proj, year = 2025, as = "dtplyr")
}
}
\seealso{
\code{\link[=run_pipeline_project]{run_pipeline_project()}} which writes the datasets this reads.
}
