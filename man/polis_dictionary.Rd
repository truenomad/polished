% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{polis_dictionary}
\alias{polis_dictionary}
\title{POLIS data dictionary (raw or cleaned schema)}
\usage{
polis_dictionary(type = c("clean", "raw"), table = NULL)
}
\arguments{
\item{type}{Which dictionary to return:
\itemize{
\item \code{"clean"} (default) -- one row per column the cleaners emit (the
canonical \code{Snake_Name}s, including the derived and indicator columns).
Excludes raw fields the cleaners drop and the raw poliovirus columns
outside the curated \code{\link[=clean_virus]{clean_virus()}} subset.
\item \code{"raw"} -- one row per raw POLIS API column \code{\link[=get_polis_data]{get_polis_data()}}
downloads, with the \code{Snake_Name} each is renamed to.
}}

\item{table}{Optional POLIS source table(s) to keep (e.g. \code{"Case"},
\code{"EnvSample"}). \code{NULL} (default) returns every table.}
}
\value{
A tibble with three columns: \code{data_type} (the POLIS source
table/stream the column belongs to, e.g. \code{"Case"}, \code{"EnvSample"},
\code{"Indicators"}), \code{column_name} (the raw \code{API_Name} when \code{type = "raw"}, the
cleaned \code{Snake_Name} when \code{type = "clean"}) and \code{label} (the description).
}
\description{
Returns the packaged data dictionary as either the raw POLIS download schema
or the cleaned-output schema. Both are views of the same \code{\link[=polis_crosswalk]{polis_crosswalk()}}.
}
\examples{
head(polis_dictionary("clean"))
head(polis_dictionary("raw", table = "Case"))

}
\seealso{
\code{\link[=polis_crosswalk]{polis_crosswalk()}}, the full raw-to-clean mapping this reads.
}
