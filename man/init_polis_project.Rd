% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{init_polis_project}
\alias{init_polis_project}
\title{Create (or re-open) a data project}
\usage{
init_polis_project(root, gitignore = TRUE, quiet = FALSE)
}
\arguments{
\item{root}{Path to the project root. Created (recursively) if absent.}

\item{gitignore}{Write a \code{.gitignore} ignoring the regenerable/precious-but-
bulky zones (\verb{raw/}, \verb{cache/}, \verb{logs/}) when one is not already present.
Default \code{TRUE}.}

\item{quiet}{Suppress the success message. Default \code{FALSE}.}
}
\value{
A \code{polis_project}: a list with \code{root} and one absolute path per zone
(\code{raw}, \code{processed}, \code{validation}, \code{cache}, \code{logs}), class \code{polis_project}.
}
\description{
Sets up the standard project layout under \code{root} and returns a \code{polis_project}
describing it. The layout has four role-named zones plus logs:
\describe{
\item{\code{raw}}{downloaded source tables and a snapshot \code{manifest.json}
(precious -- never written by cleaning).}
\item{\code{processed}}{cleaned analytic tables, one partitioned parquet dataset
per table (derived).}
\item{\code{validation}}{data-quality reports and checks.}
\item{\code{cache}}{regenerable process caches (e.g. campaign-round clustering).}
\item{\code{logs}}{run logs.}
}
Creation is idempotent and never deletes: calling it again on an existing
project just re-opens it. Downstream functions take the returned object via
their \code{project} argument; nothing relies on a hidden global.
}
\examples{
proj <- init_polis_project(file.path(tempdir(), "polis_demo"), quiet = TRUE)
proj$processed

}
