% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_es.R
\name{validate_es_sites}
\alias{validate_es_sites}
\title{Flag ES site names absent from a reference site list}
\usage{
validate_es_sites(data, sites, site_col = "site_name", verbose = TRUE)
}
\arguments{
\item{data}{A cleaned ES data frame (canonical names) carrying \code{site_name}.}

\item{sites}{Reference site list: a data frame with a site-name column, or a
character vector of known site names.}

\item{site_col}{Name of the site-name column in \code{data} and \code{sites}
(default \code{"site_name"}).}

\item{verbose}{Emit a cli warning when unknown sites are found. Default
\code{TRUE}.}
}
\value{
\code{data}, unchanged, with attribute \code{"polis_new_sites"}: a tibble with
columns \code{site_name} (character, the raw site label from \code{data}) and
\code{no_coords} (logical, \code{TRUE} when the site lacks a coordinate value,
\code{NA} when no coordinate column is present in \code{data}), one row per distinct
unmatched site.
}
\description{
Diagnostic check: environmental site names present in \code{data} but missing from
the reference \code{sites} list are flagged -- particularly new sites that also
lack coordinates, which usually signal a data-entry issue rather than a
genuine new site. Names are compared upper-cased and whitespace-squished
(embedded newlines collapsed) on both sides. \code{data} is returned unchanged,
with the unmatched sites attached as the \code{"polis_new_sites"} attribute, so the
check composes into \code{\link[=clean_es]{clean_es()}} without writing to disk or touching global
state.
}
\examples{
es <- data.frame(
  site_name = c("SITE A", "SITE B"),
  site_y_coordinate = c(6.5, NA)
)
out <- validate_es_sites(es, sites = "SITE A")
attr(out, "polis_new_sites")

}
