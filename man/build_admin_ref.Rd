% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epid_geo.R
\name{build_admin_ref}
\alias{build_admin_ref}
\title{Build an EPID -> admin-value reference (most-recent-per-EPID)}
\usage{
build_admin_ref(data, admin_col, epid_var = "epid", year_var = "year")
}
\arguments{
\item{data}{Data frame containing \code{epid_var}, \code{year_var}, and \code{admin_col}.}

\item{admin_col}{Name of the admin column to summarise.}

\item{epid_var}{EPID column name. Default \code{"epid"}.}

\item{year_var}{Recency column name. Default \code{"year"}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{epid_var} and \code{admin_col},
one row per EPID.
}
\description{
For each EPID, takes the most recent non-blank value of \code{admin_col}
(ties broken deterministically). Used to fill missing admin values from
other records that share the exact same EPID.
}
\examples{
cases <- tibble::tibble(
  epid = c("A-1", "A-1", "B-2"),
  year = c(2023, 2024, 2024),
  district = c(NA, "BOSSO", "LUANDA")
)
build_admin_ref(cases, "district")
}
