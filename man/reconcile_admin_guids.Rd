% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{reconcile_admin_guids}
\alias{reconcile_admin_guids}
\title{Reconcile case admin names and GUIDs against the long district shape}
\usage{
reconcile_admin_guids(
  data,
  shape,
  year_var = "year_onset",
  guid_vars = c(adm0 = "adm0_guid", adm1 = "adm1_guid", adm2 = "adm2_guid"),
  name_vars = c(adm0 = "adm0", adm1 = "adm1", adm2 = "adm2"),
  sink = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A case data frame (e.g. the output of \code{\link[=clean_afp]{clean_afp()}}).}

\item{shape}{The long ADM2 attribute table: a data frame with \code{adm0}/\code{adm1}/
\code{adm2}, \code{adm0_guid}/\code{adm1_guid}/\code{adm2_guid} and \code{active_year} (an \code{sf}
object is accepted and its geometry dropped).}

\item{year_var}{Case onset-year column. Default \code{"year_onset"}.}

\item{guid_vars}{Named (\code{adm0}/\code{adm1}/\code{adm2}) case GUID columns. Default
\code{c(adm0 = "adm0_guid", adm1 = "adm1_guid", adm2 = "adm2_guid")}.}

\item{name_vars}{Named (\code{adm0}/\code{adm1}/\code{adm2}) case admin-name columns.
Default \code{c(adm0 = "adm0", adm1 = "adm1", adm2 = "adm2")}.}

\item{sink}{Optional file path; when set, the per-row reconciliation flags
for changed/unresolved rows are written there as CSV.}

\item{verbose}{Emit a cli summary. Default \code{TRUE}.}
}
\value{
\code{data} with reconciled admin name/GUID columns and an added
\code{geo_source} factor (\code{guid_match} / \code{guid_corrected_from_name} /
\code{unresolved}). A \code{reconcile_qa} attribute carries per-country issue counts.
}
\description{
Validates and, where possible, corrects the admin GUIDs and names on a case
table against the authoritative long ADM2 attribute table produced by
\code{\link[=process_spatial]{process_spatial()}} / \code{\link[=create_long_shape]{create_long_shape()}} (one row per district per active
year, with the parent ADM0/ADM1 names and GUIDs). For each case:
\enumerate{
\item If the ADM2 GUID matches a district valid in the case's onset year,
the shape is authoritative: missing/mismatched admin names and parent
GUIDs are filled from it.
\item Otherwise, if the ADM0+ADM1+ADM2 names unambiguously identify one
district that year, its GUIDs are adopted (\code{guid_corrected_from_name}).
\item Otherwise the row is left unchanged and flagged \code{unresolved}.
}
GUIDs are compared case- and brace-insensitively (\code{{ABC}} == \code{abc}) and
emitted lower-case without braces. A missing onset year matches the \code{9999}
catch-all rows in the shape.
}
\examples{
shape <- data.frame(
  adm0 = "NIGERIA", adm1 = "BORNO", adm2 = "BOSSO",
  adm0_guid = "{A0}", adm1_guid = "{A1}", adm2_guid = "{A2}",
  active_year = 2024
)
cases <- data.frame(
  adm0 = "NIGERIA", adm1 = NA, adm2 = NA,
  adm0_guid = "a0", adm1_guid = NA, adm2_guid = "a2",
  year_onset = 2024
)
reconcile_admin_guids(cases, shape, verbose = FALSE)[, c("adm1", "adm2")]

}
