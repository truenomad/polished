% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_lqas_im.R
\name{process_lqas}
\alias{process_lqas}
\title{Process raw LQAS lots into classifications and district pass rates}
\usage{
process_lqas(
  lqas,
  cfg = polis_active_config(),
  shape = NULL,
  default_checked = 60,
  multiple_of = 60,
  enforce_since = 2019,
  pass_threshold = 0.9,
  warn_threshold = 0.8,
  verbose = TRUE,
  summary = TRUE
)
}
\arguments{
\item{lqas}{Raw LQAS table (data.frame/tibble), one row per lot.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object (default \code{polis_config()}); its
\code{crosswalk} maps the raw column names to canonical snake_case.}

\item{shape}{Optional district shape (an \code{sf} polygon layer or a long ADM2
attribute table) for admin GUID reconciliation via
\code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}} (keyed on \code{year}). Default \code{NULL}.}

\item{default_checked}{Lot size assumed when the sample size is missing
(default \code{60}, per POLIS).}

\item{multiple_of}{Sample-size modulus enforced from \code{enforce_since}
(default \code{60}).}

\item{enforce_since}{Year from which the multiple-of rule makes a lot INVALID
in the \emph{derived} classification (default \code{2019}).}

\item{pass_threshold}{Coverage (vaccinated fraction) at/above which a lot is
a Pass in the \emph{derived} classification (default \code{0.90}).}

\item{warn_threshold}{Coverage band for the derived 3-level "Intermediate"
class (default \code{0.80}). Lots in \verb{[warn_threshold, pass_threshold)} are
Intermediate (3-level) / Fail (2-level). Set to \code{NULL} to disable the
Intermediate band, collapsing the derived classes to strict Pass/Fail.}

\item{verbose}{Emit cli progress + the one-line roll-up result (default
\code{TRUE}).}

\item{summary}{Emit the full cli summary panel (rules + class breakdown).
Default \code{TRUE}; \code{\link[=run_pipeline]{run_pipeline()}} passes \code{FALSE} to keep the pipeline terse.}
}
\value{
A list with \code{lots} (the cleaned lot-level grain carrying both
\code{lqas2_polis} / \code{lqas3_polis} and the derived \code{coverage} / \code{invalid} /
\code{lqas2_derived} / \code{lqas3_derived}), \code{district} (per-district roll-up with
\code{n_lots}, the POLIS \code{n_pass_polis} / \code{n_fail_polis} / \code{n_invalid_polis} /
\code{pass_pct_polis} and the derived \code{n_pass_derived} / \code{n_fail_derived} /
\code{n_invalid_derived} / \code{pass_pct_derived}), and \code{meta}.
}
\description{
Cleans the raw POLIS \code{Lqas} table with the same recipe as \code{\link[=clean_afp]{clean_afp()}} /
\code{\link[=clean_sia]{clean_sia()}} -- standardise names via the crosswalk, clean strings, parse and
sanitise dates, fix admin names and (when a \code{shape} is supplied) reconcile
admin GUIDs, then dedup by \code{id} -- and rolls the cleaned lots up to a
per-district-year table, classifying each lot \strong{two ways}:
\itemize{
\item \emph{POLIS} -- the classification POLIS already ships in the download
(\code{lqas2_classification_name} / \code{lqas3_classification_name}), derived from
its unexposed \code{REF_LQASThresholds} lookup. This is the faithful answer.
\item \emph{derived} -- a transparent re-derivation from coverage
(\code{1 - children_found_unvaccinated / children_checked}) against
\code{pass_threshold} / \code{warn_threshold}, plus the documented default-60 and
2019 "sample size must be a multiple of 60 or INVALID" rules. Provided for
QA comparison against the POLIS classes.
}
}
\examples{
lqas <- data.frame(
  Id = 1L, Admin0Name = "NIGERIA", Admin1Name = "KANO",
  Admin2Name = "NASSARAWA", Admin2GUID = "{A2}", Year = 2024L,
  ChildrenChecked = 60L, ChildrenFoundUnvaccinated = 3L,
  Lqas2ClassificationName = "Pass", Lqas3ClassificationName = "Pass"
)
process_lqas(lqas, verbose = FALSE)$district

}
