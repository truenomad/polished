% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sia_quality.R
\name{process_lqas}
\alias{process_lqas}
\title{Process raw LQAS lots into classifications and district pass rates}
\usage{
process_lqas(
  lqas,
  adm2_guid_var = "Admin2Guid",
  adm2_name_var = "Admin2Name",
  adm1_name_var = "Admin1Name",
  adm0_name_var = "Admin0Name",
  date_var = "ActivityStart",
  checked_var = "ChildrenChecked",
  unvacc_var = "ChildrenUnvaccinated",
  lot_var = NULL,
  default_checked = 60,
  multiple_of = 60,
  enforce_since = 2019,
  pass_threshold = 0.9,
  warn_threshold = 0.8,
  verbose = TRUE
)
}
\arguments{
\item{lqas}{Raw LQAS table (data.frame/tibble), one row per lot.}

\item{adm2_guid_var, adm2_name_var}{Column names for the district GUID/name.}

\item{adm1_name_var, adm0_name_var}{Optional province / country name columns
(set to \code{NULL} if absent).}

\item{date_var}{Column with the lot's planned/assessment date (used to apply
the 2019 rule by year).}

\item{checked_var}{Column with the number of children checked / sample size.}

\item{unvacc_var}{Column with the number of children found unvaccinated.}

\item{lot_var}{Optional lot-identifier column (set \code{NULL} to auto-number).}

\item{default_checked}{Lot size assumed when \code{checked_var} is missing
(default \code{60}, per POLIS).}

\item{multiple_of}{Sample-size modulus enforced from \code{enforce_since}
(default \code{60}).}

\item{enforce_since}{Year from which the multiple-of rule makes a lot INVALID
(default \code{2019}).}

\item{pass_threshold}{Coverage (vaccinated fraction) at/above which a lot is
a Pass (default \code{0.90}). \strong{Transparent stand-in for \code{REF_LQASThresholds}.}}

\item{warn_threshold}{Optional coverage band for the 3-level "Intermediate"
class (default \code{0.80}). Lots in \verb{[warn_threshold, pass_threshold)} are
Intermediate (3-level) / Fail (2-level).}

\item{verbose}{Emit a cli summary (default \code{TRUE}).}
}
\value{
A list with \code{lots} (lot-level tibble with \code{coverage}, \code{invalid},
\code{lqas_class}, \code{lqas2}, \code{lqas3}), \code{district} (per-district roll-up with
\code{n_lots}, \code{n_pass}, \code{n_fail}, \code{n_invalid}, \code{pass_pct}), and \code{meta}.
}
\description{
Turns the raw \code{Lqas} table into lot-level classifications and a per-district
roll-up. Reproduces the POLIS rules that are well defined (default lot size
60; the 2019+ "sample size must be a multiple of 60 or the lot is INVALID"
rule; 2-level and 3-level recodes; pass\% excluding INVALID lots).
}
