% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epid_geo.R
\name{resolve_epid_country}
\alias{resolve_epid_country}
\title{Resolve an EPID country code to a country name}
\usage{
resolve_epid_country(
  epid,
  ref = NULL,
  region = NULL,
  code_var = "code",
  name_var = "name",
  iso3_var = "iso3",
  region_var = NULL
)
}
\arguments{
\item{epid}{Character vector of EPID strings.}

\item{ref}{Optional crosswalk data frame. When \code{NULL}, the raw code is
returned with \code{resolved = FALSE} (no fabrication).}

\item{region}{Optional region value to filter \code{ref} by (needs
\code{region_var}).}

\item{code_var}{Crosswalk column holding the country code. Default
\code{"code"}.}

\item{name_var}{Crosswalk column holding the country name. Default
\code{"name"}.}

\item{iso3_var}{Crosswalk column holding the ISO3 code. Default \code{"iso3"}.}

\item{region_var}{Optional crosswalk column holding the region. Default
\code{NULL}.}
}
\value{
A \link[tibble:tibble]{tibble} row-aligned to \code{epid} with columns
\code{code}, \code{name}, \code{iso3}, \code{n_matches}, \code{ambiguous}, \code{resolved}.
}
\description{
Maps the 3-character country code (see \code{\link[=epid_country_code]{epid_country_code()}}) to a name
and ISO3 via a caller-supplied crosswalk, matching the code against either
the crosswalk's code or ISO3 column. Resolution never guesses: a code that
maps to more than one distinct name is flagged ambiguous and left \code{NA}.
}
\details{
Temporal validity is the caller's responsibility — pre-filter \code{ref} to the
period of interest (or pass a \code{region} with \code{region_var}) before calling.
}
\examples{
crosswalk <- tibble::tibble(
  code = c("NIE", "AGO"),
  name = c("NIGERIA", "ANGOLA"),
  iso3 = c("NGA", "AGO")
)
resolve_epid_country(c("NIE-BOS-1", "AGO-LUA-1"), ref = crosswalk)
}
