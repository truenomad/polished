% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{auto_parse_types}
\alias{auto_parse_types}
\title{Infer column types after cleaning, then optionally layer factor detection}
\usage{
auto_parse_types(
  data,
  max_levels = 50,
  max_unique_ratio = 0.2,
  protect_patterns = c("id$", "uid$", "code$", "ref$", "key$"),
  keep_leading_zero_chars = TRUE,
  apply = TRUE,
  return = c("data", "both", "plan")
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{max_levels}{Maximum distinct values for a factor candidate. Default 50.}

\item{max_unique_ratio}{Maximum unique/non-NA ratio for a factor. Default
\code{0.2}.}

\item{protect_patterns}{Regexes for names kept as character. Default
\code{c("id$", "uid$", "code$", "ref$", "key$")}.}

\item{keep_leading_zero_chars}{Keep a character column when any value is a
leading-zero digit string (e.g. \code{"00123"}). Default \code{TRUE}.}

\item{apply}{If \code{TRUE} (default) apply factor conversions on top of the
parsed base types; if \code{FALSE} parse base types only.}

\item{return}{One of \code{"data"}, \code{"both"}, \code{"plan"}. Default \code{"data"}.}
}
\value{
Depending on \code{return}: the parsed tibble (\code{"data"}), the type plan
(\code{"plan"}), or \code{list(plan, data)} (\code{"both"}).
}
\description{
POLIS returns every field as character. This parses character columns to
their natural base type (numeric, integer, date, datetime, logical) via
\code{\link[readr:type_convert]{readr::type_convert()}}, protecting identifier-like names and leading-zero
codes from coercion, and optionally proposes low-cardinality character
columns as factors. The cleaners (\code{\link[=clean_afp]{clean_afp()}} etc.) call it with
\code{apply = FALSE} (base types only) when \code{polis_config(parse_types = TRUE)}.
}
\examples{
df <- tibble::tibble(id = c("001", "002"), age = c("1", "2"))
auto_parse_types(df, apply = FALSE)

}
