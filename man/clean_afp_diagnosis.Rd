% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_afp_diagnosis.R
\name{clean_afp_diagnosis}
\alias{clean_afp_diagnosis}
\title{Harmonise the AFP clinical diagnosis}
\usage{
clean_afp_diagnosis(data)
}
\arguments{
\item{data}{A cleaned AFP data frame carrying any of the canonical diagnosis
columns (\code{diagnosis_final}, \code{diagnosis_other}, \code{diagnosis_other_specified},
\code{provisional_diagnosis}) and ideally \code{classification}. When none are
present the input is returned unchanged.}
}
\value{
\code{data} with the harmonised diagnosis columns added:
\itemize{
\item \code{diagnosis_harmonised} -- the single canonical diagnosis label;
\item \code{diagnosis_source} -- which field supplied it (\code{classification (polio)}, \code{diagnosis_final}, \code{diagnosis_other (ICD)},
\code{diagnosis_other_specified}, \code{provisional_diagnosis}, \verb{non-specific text}, \verb{recorded unknown} or \code{none});
\item \code{diagnosis_class} -- the coarse class from
\code{\link[=polis_afp_diagnosis_class]{polis_afp_diagnosis_class()}};
\item \code{is_non_afp} -- \code{TRUE} for reported illness that is not acute
flaccid paralysis (malaria, sepsis, malnutrition, ...);
\item \code{residual_paralysis} -- the 60-day outcome from \code{followup_findings}
(\code{residual} / \code{recovered} / \code{died} / \code{pending}), when present;
\item \code{febrile_asymmetric_onset} -- \code{TRUE} when paralysis was asymmetric
with fever at onset, when both source columns are present.
}
The raw \verb{diagnosis_*} and \code{provisional_diagnosis} columns are left
untouched.
}
\description{
Coalesces the four scattered POLIS diagnosis fields into one canonical
\code{diagnosis_harmonised} and derives the analytic variables that separate true
non-AFP illness from the acute-flaccid-paralysis differentials. The four
sources are read in priority order -- a confirmed-polio override from
\code{classification}, then coded \code{diagnosis_final}, then the ICD-10
\code{diagnosis_other}, then the free-text \code{diagnosis_other_specified}, then the
free-text \code{provisional_diagnosis} -- and the first that resolves to a
specific diagnosis wins. Free text is normalised (accent-stripped,
lower-cased, punctuation collapsed) and matched against
\code{\link[=polis_afp_diagnosis_lookup]{polis_afp_diagnosis_lookup()}}; ICD-10 codes against \code{\link[=polis_afp_icd10]{polis_afp_icd10()}};
the resulting label is classified via \code{\link[=polis_afp_diagnosis_class]{polis_afp_diagnosis_class()}}.
}
\details{
Cases that carry text but resolve to nothing specific are labelled
\code{"Other (non-specific)"}, an explicit \code{"Unknown"} is preserved, and a case
with no diagnostic information at all becomes \code{"Not recorded"}, so every row
receives a definite label and a \code{diagnosis_source} provenance.
}
\examples{
clean_afp_diagnosis(data.frame(
  classification = c("Confirmed (wild)", "Discarded", "Discarded"),
  diagnosis_final = c(NA, "Guillain Barre Syndrom", "Other"),
  diagnosis_other = c(NA, NA, "B54"),
  diagnosis_other_specified = c(NA, NA, "malaria"),
  provisional_diagnosis = c(NA, NA, NA)
))

}
\seealso{
\code{\link[=polis_afp_diagnosis_lookup]{polis_afp_diagnosis_lookup()}}, \code{\link[=polis_afp_icd10]{polis_afp_icd10()}},
\code{\link[=polis_afp_diagnosis_class]{polis_afp_diagnosis_class()}}, \code{\link[=clean_afp_classification]{clean_afp_classification()}}.
}
