% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_es.R
\name{es_missingness}
\alias{es_missingness}
\title{Summarise missingness in key ES surveillance variables}
\usage{
es_missingness(data, vars = NULL)
}
\arguments{
\item{data}{A cleaned ES data frame.}

\item{vars}{Character vector of columns to summarise. Default \code{NULL} uses the
key ES surveillance fields that are present in \code{data}.}
}
\value{
A tibble with columns \code{variable}, \code{n}, \code{n_missing} and \code{pct_missing},
ordered most-missing first.
}
\description{
A tidy, in-memory replacement for the side-file missingness export: returns
one row per variable with the count and percentage of missing values, so a
caller can inspect or write it however they like.
}
\examples{
es <- data.frame(
  collection_date = as.Date(c("2024-01-01", NA)),
  adm0 = c("CHAD", "CHAD"),
  classification_all = c(NA, "NEGATIVE")
)
es_missingness(es)

}
