% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_pipeline.R
\name{load_polished}
\alias{load_polished}
\title{Read a country / period slice of the polished outputs}
\usage{
load_polished(
  country = NULL,
  year = NULL,
  datasets = NULL,
  output_dir = cfg$output_dir,
  cfg = polis_active_config()
)
}
\arguments{
\item{country}{Optional country/ISO3 value(s) to keep. Matched against the
first present of \code{iso3}, \code{country_iso3code}, \code{country} in each table, so an
ISO3 like \code{"AFG"} is expected. The \code{adm0} \emph{name} column is intentionally
not used for this filter. \code{NULL} keeps all.}

\item{year}{Optional year(s) to keep. Matched against the first present of
\code{year}, \code{year_onset}, \code{year_start}, \code{year_collection}, \code{collect_yr}. \code{NULL}
keeps all.}

\item{datasets}{Optional character vector restricting which datasets to read
(by output key, e.g. \code{"afp"}, \code{"es"}, \code{"virus"}). \code{NULL} (default) reads
every \verb{polished_*} file present.}

\item{output_dir}{Directory the outputs were written to; defaults to
\code{cfg$output_dir}. The files are read from its \verb{data/} sub-directory.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object; defaults to the session-active config
(\code{\link[=polis_active_config]{polis_active_config()}}), so \code{load_polished(country = "AFG")} works when
the config carries \code{output_dir}.}
}
\value{
A named list of filtered tibbles, one per \verb{polished_*} dataset found.
}
\description{
Reads the \verb{polished_*} files \code{\link[=run_pipeline]{run_pipeline()}} wrote to the \verb{data/}
sub-directory of \code{output_dir} and returns them filtered to a country and/or
year, as a named list (one element per dataset).
}
\examples{
\dontrun{
afg <- load_polished(country = "AFG")
afg$afp
load_polished(country = "AFG", year = 2024, datasets = "afp")$afp
}
}
\seealso{
\code{\link[=run_pipeline]{run_pipeline()}}, which writes the files this reads.
}
