% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_pop.R
\name{clean_pop}
\alias{clean_pop}
\title{Clean POLIS population and (optionally) reconcile against WorldPop}
\usage{
clean_pop(
  population,
  cfg = polis_active_config(),
  shape = NULL,
  worldpop = NULL,
  years = 2010:2027,
  thresholds = list(ratio_lo = 1/3, ratio_hi = 3, mad_k = 5, min_votes = 1L),
  reference_date = Sys.Date(),
  verbose = TRUE
)
}
\arguments{
\item{population}{A raw POLIS Population data frame (columns \code{PlaceId},
\code{PlaceDisplayName}, \code{Year}, \code{AgeGroupName}, \code{Value}), or a path to one.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object; defaults to \code{\link[=polis_active_config]{polis_active_config()}}.
Only its presence is required -- clean_pop reads no scoping from it (pop is
foundational/global), but it keeps the cleaner signature uniform.}

\item{shape}{Optional \strong{already-processed} district shape (an \code{sf} polygon
layer or its long ADM2 attribute table, or a path to one). Supplies the
adm0/adm1 parents POLIS lacks, the boundary-validity windows used for the
roll-ups, and the universe of districts. With no shape the output is keyed on
\code{adm2_guid} only and cannot be rolled up. Default \code{NULL}.}

\item{worldpop}{Optional named list with elements \code{all}, \code{u5}, \code{u15}. Each is
either a \strong{directory} of annual WorldPop GeoTIFFs (one per year, the year in
the file name; zonal-summed to the \code{shape} via \code{terra} + \code{exactextractr} --
both optional Suggests), \strong{or} a pre-extracted adm2-by-year table (data
frame or path) carrying \code{adm2_guid}, \code{year} and a population column. \code{NULL}
(default) runs the POLIS-only path.}

\item{years}{Calendar years to keep (POLIS carries 1990-2034 incl. projections).
Default \code{2010:2027}.}

\item{thresholds}{Named list of the implausibility tunables: \code{ratio_lo} /
\code{ratio_hi} (a POLIS value below/above this fold of WorldPop is implausible),
\code{mad_k} (scaled-MAD distance from the district's own median), \code{min_votes}
(how many signals must fire to call a value suspect). Default
\code{list(ratio_lo = 1/3, ratio_hi = 3, mad_k = 5, min_votes = 1L)}.}

\item{reference_date}{Date treated as "today" when deciding which boundary
versions are \emph{current} for the orphan-GUID name crosswalk. Default
\code{\link[=Sys.Date]{Sys.Date()}}.}

\item{verbose}{Emit cli progress headers. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{\code{adm2}}{district x year, wide: the id columns plus, per age band
(\code{u5}/\code{u15}/\code{all}), \verb{<age>_pop} (chosen), \verb{<age>_pop_polis},
\verb{<age>_pop_wp}, \verb{<age>_pop_source} (\code{polis}/\code{worldpop}/\code{district_trend}/
\code{adm1}/\code{adm0}) and \verb{<age>_pop_imputed}, plus \code{age_order_bad}. Restricted
to the boundary valid each year (no double-counting versioned shapes).}
\item{\code{adm1}, \code{adm0}}{province / country roll-ups (sums) of the nine pop
columns.}
\item{\code{meta}}{a list (skipped by the file writer): \code{audit} (one row per
district x year x age, with every signal flag), \code{dup_conflicts},
\code{orphan_xwalk}, \code{params}.}
}
}
\description{
Builds adm0/adm1/adm2 population denominators (under-5, under-15, all-ages)
from the raw POLIS Population table. Used standalone it cleans POLIS by itself
-- collapses duplicate \verb{(place, year)} rows to their median, drops zeros/blanks
to missing, flags values that jump from a district's own history, and fills
gaps from a district -> province -> country ladder. Given \code{worldpop} it adds a
cross-source layer: each POLIS value is checked against WorldPop and a
missing/implausible one is replaced (WorldPop first, then the same ladder).
}
\examples{
pop_raw <- data.frame(
  PlaceId = "0cda1c45-9529-4188-aaaa-000000000001",
  PlaceDisplayName = "SOMEWHERE",
  Year = 2020, AgeGroupName = "0 to 15 years", Value = 1000,
  check.names = FALSE
)
res <- clean_pop(pop_raw, years = 2020, verbose = FALSE)
names(res)

}
\seealso{
\code{\link[=run_pipeline]{run_pipeline()}}, which runs this as the \code{population} stream;
\code{\link[=checks_pop]{checks_pop()}}, which turns the result into a data-quality workbook.
}
