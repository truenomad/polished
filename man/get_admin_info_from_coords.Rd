% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{get_admin_info_from_coords}
\alias{get_admin_info_from_coords}
\title{Recover administrative info for point data via a spatial join}
\usage{
get_admin_info_from_coords(
  data,
  shp_adm2,
  year_col = NULL,
  lon_var = "longitude",
  lat_var = "latitude",
  crs = 4326
)
}
\arguments{
\item{data}{A data frame with \code{lon_var} and \code{lat_var} columns, and optionally
\code{adm0} and a \code{year_col}.}

\item{shp_adm2}{An \code{sf} object of district boundaries carrying the canonical
admin name columns (\code{adm0} / \code{adm1} / \code{adm2}) and GUID columns, and, for
temporal filtering, \code{year_start} / \code{year_end}.}

\item{year_col}{Optional name of the year column in \code{data}. When supplied and
present, the join is filtered to temporally valid shapes. Default \code{NULL}.}

\item{lon_var}{Longitude column name. Default \code{"longitude"}.}

\item{lat_var}{Latitude column name. Default \code{"latitude"}.}

\item{crs}{CRS of the input coordinates as an EPSG code. Default \code{4326}.}
}
\value{
A data frame (geometry dropped) with imputed \code{adm1} / \code{adm1_guid} /
\code{adm2} / \code{adm2_guid} where they were missing, restricted to rows with an
unambiguous district match.
}
\description{
Builds point geometries from a data frame's longitude/latitude columns,
spatially joins them to a district (\code{adm2}) shape table, optionally filtering
the join to shapes whose validity window (\code{year_start}..\code{year_end}) contains
the point's year, and fills any missing admin names/GUIDs from the matched
shape. Points that match more than one shape (e.g. on a boundary) are dropped
rather than guessed.
}
\examples{
\dontrun{
get_admin_info_from_coords(case_points, shp_adm2, year_col = "year_onset")
}

}
