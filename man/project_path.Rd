% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_path}
\alias{project_path}
\title{Build a path inside a project zone}
\usage{
project_path(project, zone = "root", ...)
}
\arguments{
\item{project}{A \link{polis_project}.}

\item{zone}{One of \code{"root"}, \code{"raw"}, \code{"processed"}, \code{"validation"},
\code{"cache"}, \code{"logs"}. Default \code{"root"}.}

\item{...}{Further path components appended under the zone.}
}
\value{
A single file path string.
}
\description{
Resolves a path under one of a project's zones. \code{project_path(proj, "processed", "sia")} returns \verb{<root>/processed/sia}. With no extra parts it
returns the zone directory itself.
}
\examples{
proj <- init_polis_project(file.path(tempdir(), "polis_demo"), quiet = TRUE)
project_path(proj, "processed", "sia")

}
