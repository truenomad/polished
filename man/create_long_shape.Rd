% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{create_long_shape}
\alias{create_long_shape}
\title{Expand admin shapes to one row per active year}
\usage{
create_long_shape(data, level, checks_dir = NULL)
}
\arguments{
\item{data}{An \code{sf} object (or data frame) carrying the admin name/GUID
columns plus \code{year_start} and \code{year_end}.}

\item{level}{Administrative level, \code{"adm1"} or \code{"adm2"}. Determines the
grouping columns.}

\item{checks_dir}{Optional directory for the multiple-shape check CSV. When
\code{NULL} (default) no check file is written.}
}
\value{
A \link[tibble:tibble]{tibble} with the grouping columns plus
\code{active_year}, \code{year_start} and \code{year_end}, one row per unit-year.
}
\description{
Turns a shape table with \code{year_start} / \code{year_end} validity windows into a
long table with one row per administrative unit per active year, spanning
each unit's earliest start to the later of its latest end and the current
year, plus a \code{9999} sentinel year used to match records with an unknown year.
When \code{checks_dir} is supplied, any administrative unit with more than one
shape active in the same year is written to a CSV for manual review.
}
\examples{
shapes <- tibble::tibble(
  adm0_guid = "g0", adm0 = "NIGERIA",
  adm1_guid = "g1", adm1 = "BORNO",
  year_start = 2018, year_end = 2020
)
create_long_shape(shapes, "adm1")

}
