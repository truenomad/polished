% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_human_spec.R
\name{clean_human_spec}
\alias{clean_human_spec}
\title{Clean POLIS human specimen (laboratory) data}
\usage{
clean_human_spec(
  data,
  cfg = polis_active_config(),
  shape = NULL,
  impute_geo = TRUE,
  cases = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A raw POLIS lab-specimen data frame.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object. Defaults to \code{\link[=polis_active_config]{polis_active_config()}} --
the config most recently built by \code{\link[=polis_config]{polis_config()}} this session -- so a
no-\code{cfg} call inherits the active session settings rather than fresh
defaults. Supply \code{cfg$qa} to route ambiguity flags.}

\item{shape}{Optional district shape used to reconcile admin names/GUIDs via
\code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}} (keyed on \code{year_collection}), exactly as
\code{\link[=clean_afp]{clean_afp()}} uses it -- a long ADM2 attribute table or the polygon layer
(the polygon is expanded to its long form here). Default \code{NULL}.}

\item{impute_geo}{If \code{TRUE} (default) specimens still missing \code{adm1}/\code{adm2}
(and their GUIDs) after reconciliation have them recovered from the EPID
prefix via \code{\link[=impute_geo_from_epid]{impute_geo_from_epid()}}, keyed on \code{year_collection}.}

\item{cases}{Optional cleaned AFP table (from \code{\link[=clean_afp]{clean_afp()}}). A specimen
reuses its parent case's EPID, so the case's fully-recovered geography is
the authoritative source for a specimen that carries no district of its own:
when supplied, the case \code{adm1}/\code{adm2} (and their GUIDs) fill blank specimen
cells by exact EPID match (a fast direct join, before the specimen-internal
prefix match). Default \code{NULL}.}

\item{verbose}{Emit cli progress messages for each phase. Default \code{TRUE}.}
}
\value{
A tibble of cleaned specimen records, one row per POLIS \code{id}, with
columns ordered identically to \code{\link[=clean_afp]{clean_afp()}} (id -> location -> time ->
classification -> dates -> other). The derived columns (\code{year_collection},
\code{month_collection}, \code{collect_to_lab}, \code{lab_to_culture}, \code{culture_to_itd},
\code{sent_to_seq_result}, \code{collect_to_seq}, \code{virus_type}, \code{vtype},
\code{classification_all}, \code{sabin1}/\code{sabin2}/\code{sabin3}, \code{npev}, \code{nvaccine},
\code{ev_detect}, \code{adequate}) are added only when their prerequisite source
columns are present in \code{data}, so a trimmed input yields a correspondingly
trimmed output rather than an error.
}
\description{
Standardises one raw POLIS lab-specimen table and derives the specimen-level
analytic variables surveillance relies on:
\itemize{
\item canonical snake_case names (via the crosswalk + janitor);
\item every collection/laboratory date parsed to \code{Date} and sanitised with
the same "sensible date" rule \code{\link[=clean_afp]{clean_afp()}} uses -- a value before the
dawn of surveillance (\code{min_year}) or in the future is a data-entry error
and is set to \code{NA}, never dropped (audit timestamps such as
\code{last_update_date} stay ISO strings for the keep-latest dedup);
\item \code{year_collection} / \code{month_collection} from the sanitised
\code{date_stool_collected}, plus the lab-turnaround intervals (in days)
\code{collect_to_lab}, \code{lab_to_culture}, \code{culture_to_itd}, \code{sent_to_seq_result}
and \code{collect_to_seq} -- the specimen timeliness measure (specimens carry
no onset date, so this replaces clean_afp()'s onset-relative intervals);
\item the AFP-style virus classification (via the shared lab-virus
classifier, the same engine \code{\link[=clean_es_classification]{clean_es_classification()}} uses): a
normalised \code{virus_type} list, \code{vtype} and the fused \code{classification_all}
label in the \strong{same} \code{WPV}/\code{cVDPV}/\code{aVDPV}/\code{iVDPV} vocabulary the AFP and
ES cleaners emit, the per-serotype Sabin flags \code{sabin1}/\code{sabin2}/\code{sabin3},
the \code{npev} / \code{nvaccine} flags and the fused \code{ev_detect} flag;
\item the specimen \code{adequate} flag (\code{1}/\code{0} from \code{adequate_specimen}),
alongside the raw \code{specimen_stool_condition_name} and
\code{adequate_specimen_with_condition};
\item country-keyed enrichment from \code{\link[=polis_country_lookup]{polis_country_lookup()}}
(\code{country_actual}, \code{risk_group}, \code{epi_zones} / \code{epi_zones_v2}) and the
\code{polio_type} serotype;
\item the same applicable geography cleaning as \code{\link[=clean_afp]{clean_afp()}}: normalised
admin names, GUID reconciliation against \code{shape} and EPID-prefix admin
recovery (both keyed on \code{year_collection}); coordinate recovery does not
apply as specimens carry no coordinates;
\item normalised admin names and one row per POLIS \code{id} (latest by
\code{last_update_date}).
}
The raw POLIS \code{virus_types}, \code{vdpv_classification}, the per-serotype result
fields and the lab-result columns are kept as-is alongside the derived
columns. The business key \code{specimen_id} + \code{adm0} is asserted as a tripwire:
violations are flagged to QA, never dropped.
}
\examples{
raw <- data.frame(
  Id = c(1, 1, 2),
  SpecimenId = c("S1", "S1", "S2"),
  Epid = c("A-1", "A-1", "B-2"),
  LastUpdateDate = c("2024-01-01", "2024-03-01", "2024-02-01"),
  DateStoolCollected = c("2024-01-05", "2024-01-05", "2024-02-09"),
  VirusTypes = c("cVDPV2", "cVDPV2", NA),
  VdpvClassification = c("Circulating", "Circulating", NA),
  SpecimenStoolConditionName = c("Good", "Good", "Poor"),
  AdequateSpecimen = c("Yes", "Yes", "No"),
  Admin0Name = c("NIGERIA", "NIGERIA", "CHAD"),
  check.names = FALSE
)
clean_human_spec(raw)

}
