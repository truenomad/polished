% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_es.R
\name{clean_es}
\alias{clean_es}
\title{Clean POLIS environmental surveillance data}
\usage{
clean_es(
  data,
  cfg = polis_config(),
  shape = NULL,
  impute_geo = TRUE,
  sites = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A raw POLIS environmental-samples data frame.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object (default \code{polis_config()}). Supply
\code{cfg$qa} to route ambiguity flags.}

\item{shape}{Optional district shape used to reconcile admin names/GUIDs via
\code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}} (keyed on \code{year_collection}), exactly as
\code{\link[=clean_afp]{clean_afp()}} uses it. Either a long ADM2 attribute table
(\code{spatial_adm2_long_shape}) or the polygon layer (\code{spatial_global_adm2}). A
polygon is expanded to its long form here to drive the GUID reconcile and
\emph{also} drives coordinate-based admin recovery: samples still missing
\code{adm1}/\code{adm2} (or their GUIDs) but carrying site coordinates have their
admin recovered by a point-in-polygon join via \code{\link[=impute_geo_from_coords]{impute_geo_from_coords()}}
(the ES counterpart of AFP EPID-prefix recovery; ES samples carry no
geocoded EPID). Default \code{NULL} (no shape-based recovery).}

\item{impute_geo}{If \code{TRUE} (default) samples still missing \code{adm2_guid} after
any shape-based recovery have their admin chain borrowed from other samples
at the same site via the self-reference fill (see details); only sites that
map unambiguously to one district are used, so conflicting sites are left
flagged rather than guessed. Needs no shape, so it runs standalone. Adds a
\code{geo_source} of \code{"site_match"} to filled rows.}

\item{sites}{Optional reference list of known environmental site names (a data
frame with a \code{site_name} column, or a character vector). When supplied,
sites absent from it are flagged via \code{\link[=validate_es_sites]{validate_es_sites()}}. Default \code{NULL}
(no site validation).}

\item{verbose}{Emit cli progress messages for each phase. Default \code{TRUE}.}
}
\value{
A tibble of cleaned ES records, one row per POLIS \code{id}, with columns
ordered identically to \code{\link[=clean_afp]{clean_afp()}} (id -> location -> time ->
classification -> dates -> other). The derived columns (\code{year_collection},
\code{month_collection}, \code{vtype}, \code{classification_all}, \code{sabin1}/\code{sabin2}/
\code{sabin3}, \code{npev}, \code{nvaccine}, \code{ev_detect}) are added only when their
prerequisite source columns are present in \code{data}, so a trimmed input yields
a correspondingly trimmed output rather than an error.
}
\description{
Standardises one raw POLIS environmental-samples table and derives the
sample-level analytic variables ES surveillance relies on:
\itemize{
\item canonical snake_case names (via the crosswalk + janitor);
\item every collection/laboratory date parsed to \code{Date} and sanitised with
the same "sensible date" rule clean_afp() uses -- a value before the dawn
of surveillance (\code{min_year}) or in the future is a data-entry error and is
set to \code{NA}, never dropped (audit timestamps such as \code{last_update_date}
stay ISO strings for the keep-latest dedup);
\item \code{year_collection} / \code{month_collection} from the sanitised
\code{collection_date};
\item the AFP-style virus classification (via \code{\link[=clean_es_classification]{clean_es_classification()}}):
a normalised \code{virus_type} list, \code{vtype} and the fused \code{classification_all}
label in the \strong{same} \code{WPV}/\code{cVDPV}/\code{aVDPV}/\code{iVDPV} vocabulary the AFP
cleaner emits (so one \code{grepl("WPV|cVDPV", classification_all)} works across
both streams), the per-serotype Sabin flags \code{sabin1}/\code{sabin2}/\code{sabin3},
the \code{npev} and \code{nvaccine} (nOPV2) flags and the fused \code{ev_detect}
"anything detected" flag;
\item the same geography cleaning as \code{\link[=clean_afp]{clean_afp()}}: normalised admin names,
canonicalised admin GUIDs (braces stripped, lower-cased, so they are
join-ready with the spatial layer), a title-cased \code{site} label, and --
when a \code{shape} is supplied -- admin-GUID reconciliation and
coordinate-based admin recovery (all keyed on \code{year_collection});
\item country-keyed enrichment from \code{\link[=polis_country_lookup]{polis_country_lookup()}} --
\code{country_actual}, \code{risk_group}, \code{epi_zones} / \code{epi_zones_v2} -- and the
\code{polio_type} serotype (the ES counterpart of the AFP enrichment; the
case-classification AFP flags do not apply to environmental samples);
\item one row per POLIS \code{id} (latest by \code{last_update_date}).
}
The raw POLIS \code{virus_types}, \code{vdpv_classifications}, the per-serotype
\verb{vaccine*}/\verb{vdpv*}/\verb{wild*} fields and \code{sample_condition} are kept as-is
alongside the derived columns. The business key \code{sample_id} + \code{adm0} (the ES
analogue of the AFP \code{epid} + \code{adm0} key, \code{sample_id} being the EPID-equivalent
sample identifier) is asserted as a tripwire: violations are flagged to QA,
never dropped. (Unlike AFP, a sample can legitimately yield several virus
detections, so this key is not collapsed.)
}
\examples{
raw <- data.frame(
  Id = c(1, 1, 2),
  EnviroSampleId = c("E1", "E1", "E2"),
  LastUpdateDate = c("2024-01-01", "2024-03-01", "2024-02-01"),
  CollectionDate = c("2024-01-05", "2024-01-05", "2024-02-09"),
  VirusTypes = c("cVDPV2", "cVDPV2", NA),
  VACCINE1 = c("No", "No", "Yes"),
  IsNPEV = c("No", "No", "Yes"),
  Admin0Name = c("NIGERIA", "NIGERIA", "CHAD"),
  check.names = FALSE
)
clean_es(raw)

}
