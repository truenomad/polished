% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{init_polis_pipeline}
\alias{init_polis_pipeline}
\title{Scaffold a full polished pipeline project}
\usage{
init_polis_pipeline(
  root,
  regions = "EMRO",
  start_year = 2020,
  pop_years = 2010:2027,
  pop_source = c("reconciled", "polis", "worldpop"),
  domains = c("shapefiles", "population", "polis", "vaccination"),
  write_rprofile = TRUE,
  write_scripts = TRUE,
  gitignore = TRUE,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{root}{Path to the project root (created recursively if absent).}

\item{regions}{WHO region codes the pipeline is scoped to, written into the
generated \code{cfg}. Default \code{"EMRO"}.}

\item{start_year}{Earliest onset/collection year to retain. Default \code{2020}.}

\item{pop_years}{Calendar years for the population / WorldPop step. Default
\code{2010:2027}.}

\item{pop_source}{Population denominator preference for \code{\link[=polis_config]{polis_config()}}, one
of \code{"reconciled"}, \code{"polis"}, \code{"worldpop"}. Default \code{"reconciled"}.}

\item{domains}{Which \verb{01_data} domains to create: any of \code{"shapefiles"},
\code{"population"}, \code{"polis"}, \code{"vaccination"}. Default all four.}

\item{write_rprofile, write_scripts, gitignore}{Whether to write the wired
\code{.Rprofile}, the starter \verb{02_scripts/}, and the \code{.gitignore}. Default
\code{TRUE}.}

\item{overwrite}{Overwrite \code{.Rprofile} / scripts / \code{.gitignore} that already
exist. Default \code{FALSE} -- existing files are kept (and skipped with a note),
so re-running on a live project never clobbers it. Directory creation is
always idempotent.}

\item{quiet}{Suppress the success message. Default \code{FALSE}.}
}
\value{
Invisibly, a list with \code{root} and the absolute \code{dirs} created.
}
\description{
Creates the domain-numbered data-pipeline layout this package is built around
(\verb{01_data/} domains, \verb{02_scripts/}, \verb{03_outputs/}) and writes a wired
\code{.Rprofile} (the \code{cfg} manifest), a \code{.gitignore}, and starter
\verb{2a_download_data.R} / \verb{2b_process_data.R} scripts. Everything the generated
\code{cfg} points at exists on disk, so after dropping the WHO polio GDB layers in
\verb{01_data/1a_shapefiles/raw/}, sourcing \verb{2a} then \verb{2b} runs the whole
download -> clean pipeline (downloads POLIS streams + WorldPop, processes the
shapefile, extracts WorldPop, and runs \code{\link[=run_pipeline]{run_pipeline()}} over every stream).
}
\details{
Distinct from \code{\link[=init_polis_project]{init_polis_project()}}, which scaffolds a lighter generic
\code{raw/processed/validation/cache/logs} layout.
}
\examples{
proj <- init_polis_pipeline(
  file.path(tempdir(), "polio_pipeline"), regions = "EMRO", quiet = TRUE
)
file.exists(file.path(proj$root, ".Rprofile"))

}
\seealso{
\code{\link[=init_polis_project]{init_polis_project()}}, \code{\link[=polis_config]{polis_config()}}, \code{\link[=run_pipeline]{run_pipeline()}}.
}
