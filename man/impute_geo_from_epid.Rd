% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epid_geo.R
\name{impute_geo_from_epid}
\alias{impute_geo_from_epid}
\title{Recover administrative geography from the EPID}
\usage{
impute_geo_from_epid(
  data,
  epid_var = "epid",
  year_var = "yronset",
  admin0_var = "place.admin.0",
  admin1_var = "place.admin.1",
  admin2_var = "place.admin.2",
  guid_vars = c(adm2 = "Admin2GUID"),
  reference = NULL,
  country_ref = NULL,
  strategies = c("self_ref", "prefix_match", "reference", "country_prefix"),
  prefix_length = 11,
  year_window = 0,
  sep = "-",
  canonicalise = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Non-empty data frame carrying an EPID column.}

\item{epid_var}{EPID column name. Default \code{"epid"}.}

\item{year_var}{Year/onset column name. Default \code{"yronset"}.}

\item{admin0_var, admin1_var, admin2_var}{Admin name columns; \code{NULL} skips a
level. Defaults \code{"place.admin.0"}/\code{.1}/\code{.2}.}

\item{guid_vars}{Named character vector mapping levels (\code{adm0}/\code{adm1}/
\code{adm2}) to GUID columns to fill. Default \code{c(adm2 = "Admin2GUID")}; set
\code{NULL} to fill no GUIDs.}

\item{reference}{Optional external table (keyed on \code{epid} or \code{prefix}) of
admin names/GUIDs, for data with no names of its own. Default \code{NULL}.}

\item{country_ref}{Optional country code -> name/ISO3 crosswalk. Default
\code{NULL}.}

\item{strategies}{Ordered subset of \code{c("self_ref", "prefix_match", "reference", "country_prefix")}. Default uses all four.}

\item{prefix_length}{Prefix length for prefix-matching. Default \code{11}.}

\item{year_window}{Allowed +/- year distance when prefix-matching. Default
\code{0}.}

\item{sep}{EPID segment delimiter. Default \code{"-"}.}

\item{canonicalise}{Whether to canonicalise filled name cells when a
canonicaliser is available. Default \code{TRUE}.}

\item{verbose}{Whether to print a cli summary. Default \code{TRUE}.}
}
\value{
A named list:
\describe{
\item{data}{\code{data} with filled admin/GUID columns plus a
\verb{<col>_source} provenance factor per filled column.}
\item{ref}{The self-reference lookups built (for audit).}
\item{qa}{A tibble of per-level fill counts and \code{pct_resolved}.}
\item{meta}{The settings used.}
}
}
\description{
Fills missing administrative names (and optionally GUIDs) using the EPID as
a recovery key, through an ordered, provenance-stamped cascade. Only blank
cells are filled; present values are never overwritten and nothing is
fabricated on ambiguity.
}
\details{
The cascade, per admin level (Admin0, then Admin1, then Admin2):
\describe{
\item{original}{Value already present — kept.}
\item{self_ref}{Most-recent non-blank value for the exact same EPID
elsewhere in \code{data}.}
\item{prefix_match}{Unique value among records sharing the geographic
prefix within \code{year_window} years; a parent-level tie-break is applied
before declaring ambiguity.}
\item{reference}{An external \code{reference} table keyed on EPID or prefix.}
\item{country_prefix}{Admin0 only — the country code resolved via
\code{country_ref}.}
}
Any cell still blank after the cascade is labelled \code{unresolved}.
}
\examples{
cases <- tibble::tibble(
  epid = c("NIE-BOS-AAA-24-001", "NIE-BOS-AAA-24-002", "AGO-LUA-BBB-24-001"),
  yronset = c(2024, 2024, 2024),
  place.admin.0 = c("NIGERIA", NA, "ANGOLA"),
  place.admin.1 = c("BORNO", NA, "LUANDA"),
  place.admin.2 = c("BOSSO", NA, "LUANDA"),
  Admin2GUID = c("g-bosso", NA, "g-luanda")
)
result <- impute_geo_from_epid(cases, verbose = FALSE)
result$qa
}
