% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{flag_ambiguous}
\alias{flag_ambiguous}
\title{Flag (do not drop) rows whose business key spans multiple Ids}
\usage{
flag_ambiguous(data, key, id = "id", sink = NULL)
}
\arguments{
\item{data}{A data frame (already deduped by \code{id}).}

\item{key}{Character vector naming the business key columns.}

\item{id}{Name of the identifier column (default \code{"id"}).}

\item{sink}{Optional destination for the flagged rows: a file path (CSV is
written) or \code{NULL} (flags are attached as the \code{polis_ambiguous} attribute).}
}
\value{
\code{data}, unchanged, possibly carrying a \code{polis_ambiguous} attribute.
}
\description{
A tripwire on the assumed business uniqueness key. After \code{\link[=polis_upsert]{polis_upsert()}} has
reduced the data to one row per \code{id}, a well-formed dataset should also be
unique on its business key. Rows that violate this are surfaced to QA -- and
left in the data -- so a genuine reclassification is never silently dropped.
}
