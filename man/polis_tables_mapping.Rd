% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_polis_data.R
\docType{data}
\name{polis_tables_mapping}
\alias{polis_tables_mapping}
\title{POLIS table catalogue}
\format{
A data.frame with one row per supported table and columns:
\describe{
\item{table_name}{Short identifier used by \code{tables = "..."} and as
the canonical filename stem.}
\item{endpoint}{OData endpoint suffix appended to
\verb{https://extranet.who.int/polis/api/v2/}.}
\item{date_field}{Column used for both the OData filter and the
dedup tiebreaker.}
}
}
\usage{
polis_tables_mapping
}
\description{
Static mapping of the tables \code{get_polis_data()} supports.
}
\details{
Each \code{date_field} is the "update" column the package uses when
filtering. Probes against POLIS confirmed each value is 100\%-populated
AND clustered post-2010 (records were imported into POLIS then), so a
filter on this field catches every row in the table including pre-2000
legacy records. The clinical/event columns (\code{CaseDate}, \code{VirusDate},
\code{CollectionDate}) are skipped because they contain pre-2000 legacy
dates that fall outside typical user-supplied ranges.

A \code{date_field} of \code{NA} marks a \strong{reference table} (e.g. \code{population}) that
carries no usable update date: it is pulled whole in a single Id-paginated
pass, ignoring \code{min_date}/\code{max_date}/\code{region}.
}
\keyword{datasets}
