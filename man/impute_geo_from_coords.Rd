% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{impute_geo_from_coords}
\alias{impute_geo_from_coords}
\title{Recover missing admin from coordinates, in place}
\usage{
impute_geo_from_coords(
  data,
  shape_adm2,
  year_var = "year_onset",
  lon_var = "longitude",
  lat_var = "latitude",
  target = c("adm1", "adm2", "adm1_guid", "adm2_guid"),
  verbose = TRUE
)
}
\arguments{
\item{data}{A case data frame with coordinate and admin columns.}

\item{shape_adm2}{An \code{sf} object of district polygons carrying the canonical
admin name + GUID columns and \code{year_start}/\code{year_end}.}

\item{year_var}{Onset-year column for temporal filtering. Default
\code{"year_onset"}.}

\item{lon_var, lat_var}{Coordinate columns. Default \code{"longitude"} /
\code{"latitude"}.}

\item{target}{Admin columns whose \code{NA} marks a case as needing recovery: a
case is recovered when \emph{any} of them is missing. Default
\code{c("adm1", "adm2", "adm1_guid", "adm2_guid")}, so a present-but-stale GUID
no longer blocks recovery of a missing name.}

\item{verbose}{Emit a cli summary. Default \code{TRUE}.}
}
\value{
\code{data} with \code{adm1}/\code{adm2}/\code{adm1_guid}/\code{adm2_guid} filled where the
coordinates resolved to a single district; all rows retained.
}
\description{
For cases still missing \code{adm2}/\code{adm2_guid} that carry valid coordinates, a
point-in-polygon join against the district polygons (\code{shape_adm2}, the
\code{spatial_global_adm2} layer from \code{\link[=process_spatial]{process_spatial()}}) recovers \code{adm1}/\code{adm2}
and their GUIDs, matched to the case's onset year. Unlike
\code{\link[=get_admin_info_from_coords]{get_admin_info_from_coords()}} every row is kept -- only the missing cells of
unambiguously matched cases are filled.
}
\examples{
\dontrun{
shp <- qs2::qs_read("spatial_global_adm2.qs2")
impute_geo_from_coords(cases, shp)
}

}
