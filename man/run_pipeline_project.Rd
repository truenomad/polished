% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{run_pipeline_project}
\alias{run_pipeline_project}
\title{Clean a project's raw tables to disk, one group at a time}
\usage{
run_pipeline_project(
  project,
  cfg = polis_config(),
  partition_by = "year",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{project}{A \code{polis_project} whose \verb{raw/} zone holds the source tables.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} (default \code{polis_config()}).}

\item{partition_by}{Columns to partition the parquet output on, when present.
Default \code{"year"} (resolved per table to its year column).}

\item{cache}{Use the project \verb{cache/} zone for the SIA round-clustering cache,
keyed on the manifest snapshot id when available. Default \code{TRUE}.}

\item{quiet}{Suppress per-table progress. Default \code{FALSE}.}
}
\value{
Invisibly, a named list with \code{snapshot_id} and a \code{tables} data frame
(one row per written dataset: \code{dataset}, \code{rows}, \code{path}).
}
\description{
The disk-streaming counterpart to \code{\link[=run_pipeline]{run_pipeline()}}: reads the raw tables from
the project's \verb{raw/} zone, cleans them and writes each as a partitioned
parquet dataset under \verb{processed/}, then returns a lightweight result manifest
-- the cleaned tables themselves never accumulate in the session.
}
\details{
To bound peak memory the human + environmental streams (which together build
the virus/positives table) are processed as one group and freed before the
independent SIA stream, so at most one group is resident at a time rather than
the whole cleaned set.
}
\examples{
\dontrun{
proj <- init_polis_project(file.path(tempdir(), "demo"), quiet = TRUE)
run_pipeline_project(proj)
}
}
\seealso{
\code{\link[=load_polis]{load_polis()}} to read a slice back, \code{\link[=run_pipeline]{run_pipeline()}} for the
in-memory variant.
}
