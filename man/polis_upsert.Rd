% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{polis_upsert}
\alias{polis_upsert}
\title{Upsert by Id, keeping the latest record}
\usage{
polis_upsert(
  store,
  pull = NULL,
  id = "id",
  date = "last_update_date",
  grain = NULL
)
}
\arguments{
\item{store}{A data frame (the accumulated store, or simply the data to
dedup).}

\item{pull}{Optional new data frame to upsert into \code{store}.}

\item{id}{Name of the canonical identifier column (default \code{"id"}).}

\item{date}{Name of the update-timestamp column used for recency
(default \code{"last_update_date"}).}

\item{grain}{Optional character vector of columns defining a finer row grain.
When supplied, exact duplicates at this grain are collapsed (keep-latest)
before the per-\code{id} step.}
}
\value{
A data frame with one row per \code{id}.
}
\description{
Combines an existing store with an optional new pull, optionally collapses
exact duplicate rows at a finer grain, then keeps exactly one row per \code{id}:
the one with the maximum \code{date}. This is unconditional (no Id-range
shortcut), so a single primitive governs recency everywhere.
}
\examples{
df <- data.frame(
  id = c(1, 1, 2),
  last_update_date = as.Date(c("2024-01-01", "2024-03-01", "2024-02-01")),
  value = c("old", "new", "x")
)
polis_upsert(df)

}
