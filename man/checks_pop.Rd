% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{checks_pop}
\alias{checks_pop}
\title{Run POLIS population data-quality checks}
\usage{
checks_pop(pop, reference_date = Sys.Date())
}
\arguments{
\item{pop}{The list returned by \code{\link[=clean_pop]{clean_pop()}} (\verb{$adm0}/\verb{$adm1}/\verb{$adm2}/\verb{$meta}).}

\item{reference_date}{Unused; accepted for a uniform \verb{checks_*()} signature.}
}
\value{
A named list: \code{summary} (one row per issue with \code{check}, \code{domain},
\code{severity}, \code{n_flagged}, \code{description}) followed by the detail tables
(\code{conflicting_dups}, \code{age_violations}, \code{orphan_guids}, \code{ratio_outliers},
\code{coverage_by_country}, \code{overrides}, \code{source_mix}). Pass it to
\code{\link[=write_checks_excel]{write_checks_excel()}}.
}
\description{
Documents every POLIS-vs-WorldPop reconciliation issue \code{\link[=clean_pop]{clean_pop()}} found and
how each was resolved, as a \verb{checks_*} result ready for \code{\link[=write_checks_excel]{write_checks_excel()}}.
Unlike the other \verb{checks_*()} functions it takes the \strong{whole \code{\link[=clean_pop]{clean_pop()}}
list} (it reads \verb{$adm2} and the \verb{$meta} audit), not a single table.
}
\examples{
pop_raw <- data.frame(
  PlaceId = "g-1", PlaceDisplayName = "X", Year = 2020,
  AgeGroupName = "0 to 15 years", Value = 1000, check.names = FALSE
)
checks_pop(clean_pop(pop_raw, years = 2020, verbose = FALSE))$summary

}
\seealso{
\code{\link[=clean_pop]{clean_pop()}}.
}
