% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sia_quality.R
\name{process_im}
\alias{process_im}
\title{Process raw Independent Monitoring (IM) data into missed-children rates}
\usage{
process_im(
  im,
  adm2_guid_var = "Admin2Guid",
  adm2_name_var = "Admin2Name",
  adm1_name_var = "Admin1Name",
  adm0_name_var = "Admin0Name",
  checked_var = "ChildrenChecked",
  marked_var = "ChildrenMarked",
  result_var = "Result",
  date_var = "ActivityStart",
  verbose = TRUE
)
}
\arguments{
\item{im}{Raw IM table (data.frame/tibble).}

\item{adm2_guid_var, adm2_name_var}{District GUID / name columns.}

\item{adm1_name_var, adm0_name_var}{Optional province / country name columns.}

\item{checked_var}{Column with children checked.}

\item{marked_var}{Column with children marked (finger-marked / vaccinated).}

\item{result_var}{Optional pre-computed per-row missed result (used as the
fallback when no children were checked).}

\item{date_var}{Optional date column (for a \code{year} grouping).}

\item{verbose}{Emit a cli summary (default \code{TRUE}).}
}
\value{
A list with \code{district} (per-district missed-children rate + status)
and \code{meta}.
}
\description{
Reproduces the POLIS IM missed-children computation: per district/parent,
\code{missed = 1 - sum(marked)/sum(checked)}, falling back to \code{mean(result)} when
no children were checked; with a Valid/Invalid status flag (Invalid when the
result is missing or negative).
}
