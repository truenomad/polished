% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_lqas_im.R
\name{process_im}
\alias{process_im}
\title{Process raw Independent Monitoring (IM) data into missed-children rates}
\usage{
process_im(
  im,
  cfg = polis_active_config(),
  shape = NULL,
  verbose = TRUE,
  summary = TRUE
)
}
\arguments{
\item{im}{Raw IM table (data.frame/tibble).}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object (default \code{polis_config()}); its
\code{crosswalk} maps the raw column names to canonical snake_case.}

\item{shape}{Optional district shape (an \code{sf} polygon layer or a long ADM2
attribute table) for admin GUID reconciliation via
\code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}} (keyed on \code{year}). Default \code{NULL}.}

\item{verbose}{Emit cli progress + the one-line roll-up result (default
\code{TRUE}).}

\item{summary}{Emit the full cli summary panel (per-setting missed-children).
Default \code{TRUE}; \code{\link[=run_pipeline]{run_pipeline()}} passes \code{FALSE} to keep the pipeline terse.}
}
\value{
A list with \code{district} (per-district-year, carrying both
\code{missed_frac_inhouse} / \code{im_status_inhouse} and \code{missed_frac_outhouse} /
\code{im_status_outhouse} plus their checked/marked totals) and \code{meta}.
}
\description{
Cleans the raw POLIS \code{Im} table with the same recipe as \code{\link[=clean_afp]{clean_afp()}} /
\code{\link[=clean_sia]{clean_sia()}} -- standardise names via the crosswalk, clean strings, parse and
sanitise dates, fix admin names and (when a \code{shape} is supplied) reconcile
admin GUIDs, then dedup by \code{id} -- and computes the missed-children fraction
per district-year \strong{separately for in-house and out-of-house monitoring}.
For each setting \code{missed = 1 - sum(marked)/sum(checked)}, falling back to
\code{mean(result)} when no children were checked, with a Valid/Invalid status
flag (Invalid when the result is missing or negative).
}
\examples{
im <- data.frame(
  Id = 1L, Admin0 = "NIGERIA", Admin1 = "KANO", Admin2 = "NASSARAWA",
  Admin2GUID = "{A2}", ActivityPlannedDateFromYear = 2024L,
  HouseholdsNumberChildrenChecked = 20L,
  HouseholdsNumberChildrenMarked = 18L, HouseholdsResult = NA_real_,
  OutOfHouseNumberChildrenChecked = 10L,
  OutOfHouseNumberChildrenMarked = 8L, OutOfHouseResult = NA_real_
)
process_im(im, verbose = FALSE)$district

}
