% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{epid_split}
\alias{epid_split}
\title{Split an EPID into its component segments}
\usage{
epid_split(
  epid,
  sep = "-",
  parts = c("country", "province", "district", "year", "serial"),
  extra = "drop",
  fill = "right"
)
}
\arguments{
\item{epid}{Character vector of EPID strings.}

\item{sep}{Single-character delimiter between segments. Default \code{"-"}.}

\item{parts}{Character vector naming the output columns, in order. Default
\code{c("country", "province", "district", "year", "serial")}.}

\item{extra}{How to treat segments beyond \code{length(parts)}: \code{"drop"}
(default) discards them; \code{"merge"} collapses the remainder into the last
column.}

\item{fill}{How to pad EPIDs with fewer segments than \code{parts}. Only
\code{"right"} is supported: missing trailing segments become \code{NA}.}
}
\value{
A \link[tibble:tibble]{tibble} with one character column per \code{parts}
element. Blank/\code{NA} EPIDs yield an all-\code{NA} row.
}
\description{
Splits each EPID on \code{sep} into one column per element of \code{parts},
preserving \code{NA} and never erroring on malformed input.
}
\examples{
epid_split(c("NIE-BOS-XYZ-24-001", "AGO-LUA", NA))
epid_split("NIE-BOS-XYZ-24-001-EXTRA", extra = "merge")
}
