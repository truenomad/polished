% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_lqas_im.R
\name{process_sia_quality}
\alias{process_sia_quality}
\title{Process the POLIS SIA campaign-quality tables (LQAS + IM)}
\usage{
process_sia_quality(
  lqas = NULL,
  im = NULL,
  cfg = polis_active_config(),
  shape = NULL,
  verbose = TRUE,
  summary = TRUE,
  ...
)
}
\arguments{
\item{lqas}{Optional raw POLIS LQAS table (data.frame). \code{NULL} (default)
skips LQAS.}

\item{im}{Optional raw POLIS IM table (data.frame). \code{NULL} (default) skips IM.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object (default \code{polis_config()}); its
\code{crosswalk} standardises the raw column names.}

\item{shape}{Optional district shape (an \code{sf} polygon layer or a long ADM2
attribute table) used to reconcile admin names/GUIDs via
\code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}}, exactly as the other cleaners use it. Default
\code{NULL} (no shape-based reconciliation).}

\item{verbose}{Emit cli progress + one-line roll-up results (default \code{TRUE}).}

\item{summary}{Emit the full cli summary panel per stream (rules, class
breakdown, mean missed-children). Default \code{TRUE} for standalone use;
\code{\link[=run_pipeline]{run_pipeline()}} passes \code{FALSE} so the pipeline stays terse and only the
key steps and roll-up counts are reported.}

\item{...}{Extra analytic arguments forwarded to \code{\link[=process_lqas]{process_lqas()}} (e.g.
\code{pass_threshold}). Each processor keeps only the arguments it accepts.}
}
\value{
A named list with \code{lqas} (the \code{\link[=process_lqas]{process_lqas()}} result, or \code{NULL}) and
\code{im} (the \code{\link[=process_im]{process_im()}} result, or \code{NULL}).
}
\description{
Thin entry point that runs \code{\link[=process_lqas]{process_lqas()}} and/or \code{\link[=process_im]{process_im()}} over the
raw POLIS \code{Lqas} and \code{Im} tables and returns their roll-ups together. Either
input may be \code{NULL}; the matching slot is then \code{NULL}. This is what
\code{\link[=run_pipeline]{run_pipeline()}} calls when an \code{lqas} / \code{im} table is present in its inputs.
}
\examples{
lqas <- data.frame(
  Id = 1L, Admin0Name = "NIGERIA", Admin1Name = "KANO",
  Admin2Name = "NASSARAWA", Admin2GUID = "{A2}", Year = 2024L,
  ChildrenChecked = 60L, ChildrenFoundUnvaccinated = 3L,
  Lqas2ClassificationName = "Pass", Lqas3ClassificationName = "Pass"
)
out <- process_sia_quality(lqas = lqas, verbose = FALSE)
out$lqas$district

}
