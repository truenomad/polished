% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_es.R
\name{clean_es_classification}
\alias{clean_es_classification}
\title{Derive the AFP-style virus classification and detection flags for ES}
\usage{
clean_es_classification(data)
}
\arguments{
\item{data}{A cleaned ES data frame carrying at least \code{virus_types} (and
ideally \code{vdpv_classifications}). Any source column may be absent -- each
derived column is added only when its inputs are present.}
}
\value{
\code{data} with \code{virus_type} (the normalised full virus-type list),
\code{vtype}, \code{classification_all}, \code{sabin1}/\code{sabin2}/\code{sabin3}, \code{npev},
\code{nvaccine} and \code{ev_detect} added where derivable; the raw POLIS columns are
left untouched.
}
\description{
The environmental analogue of \code{\link[=clean_afp_classification]{clean_afp_classification()}}: it decodes the
same poliovirus vocabulary so a single downstream filter
(\code{grepl("WPV|cVDPV", classification_all)}) works identically across the human
(AFP) and environmental streams. Detection is read from the combined
\code{virus_types} string and the \code{vdpv_classifications} field -- the ES
equivalents of POLIS \code{polio_virus_types} / \code{vdpv_classifications} -- using
standard \strong{WPV} (wild poliovirus) nomenclature, \emph{not} the legacy \verb{WILD n}
strings.
}
\details{
Two layers, mirroring the AFP cleaner:
\enumerate{
\item \code{vtype} decodes the specific poliovirus. A VDPV always carries an
explicit kind prefix from \code{vdpv_classifications} -- \code{cVDPV} (circulating),
\code{aVDPV} (ambiguous), \code{iVDPV} (immune-deficient) -- so the three are never
merged; an untyped \verb{VDPV n} only remains when the kind is unknown. Samples
with no poliovirus are \code{none} (and \code{NA} when the sample was never typed).
\item \code{classification_all} is the single analysis label: the \code{vtype} virus
string for poliovirus-positive samples, otherwise the sample outcome --
\code{SABIN} (Sabin vaccine virus only), \code{NPEV} (non-polio enterovirus only),
\code{NEGATIVE} (tested negative) or \code{PENDING} (classification pending);
samples matching none stay \code{none}/\code{NA}.
}

The decoding engine (\code{\link[=.polis_classify_virus]{.polis_classify_virus()}}) is shared with
\code{\link[=clean_human_spec]{clean_human_spec()}}: ES samples and human lab specimens have the same
lab-result structure (\code{virus_types} plus a VDPV classification, which may
arrive as the plural \code{vdpv_classifications} or the singular
\code{vdpv_classification}), so both reuse one classifier.
}
\section{Classification vocabulary (match on these prefixes, not free text)}{

\describe{
\item{Wild}{\verb{WPV 1}, \verb{WPV 2}, \verb{WPV 3}, \code{WPV1andWPV3} -- prefix \code{WPV}.}
\item{Circulating VDPV}{\verb{cVDPV 1/2/3} and combinations -- prefix \code{cVDPV}.}
\item{Ambiguous VDPV}{\verb{aVDPV 1/2/3} -- prefix \code{aVDPV}. Labelled, never
folded into \code{cVDPV}.}
\item{Immune-deficient VDPV}{\verb{iVDPV 1/2/3} -- prefix \code{iVDPV}.}
\item{Untyped VDPV}{\verb{VDPV 1/2/3} -- kind unknown.}
\item{Wild + VDPV co-detection}{\code{WPV1and...} (e.g. \verb{WPV1andcVDPV 2}).}
\item{Sample outcome}{\code{SABIN}, \code{NPEV}, \code{NEGATIVE}, \code{PENDING}, \code{none}.}
}

Alongside the labels it derives the Sabin-detection flags
\code{sabin1} / \code{sabin2} / \code{sabin3} (per serotype, exactly as the AFP cleaner), the
non-polio-enterovirus flag \code{npev}, the novel-OPV2 flag \code{nvaccine} and the
fused \code{ev_detect} ("any poliovirus or enterovirus detected"). Every condition
is NA-safe: a missing source value leaves the prior value intact rather than
nulling it. The raw \code{virus_types} and \code{vdpv_classifications} columns are kept.
}

\examples{
clean_es_classification(data.frame(
  virus_types = c("cVDPV2", "WILD1", "NPEV, VACCINE3", NA),
  vdpv_classifications = c("Circulating", NA, NA, NA),
  is_npev = c(NA, NA, TRUE, NA)
))

}
