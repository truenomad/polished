% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{process_spatial}
\alias{process_spatial}
\title{Clean WHO administrative spatial data}
\usage{
process_spatial(
  input_path,
  output_dir,
  layers = c(adm0 = "GLOBAL_ADM0", adm1 = "GLOBAL_ADM1", adm2 = "GLOBAL_ADM2"),
  transform = TRUE,
  crs = 4326,
  fix_issues = TRUE,
  sliver_area = 10000,
  output_format = "rds",
  verbose = TRUE
)
}
\arguments{
\item{input_path}{Path to the spatial source: a \code{.gdb} / \code{.gpkg} dataset, or
a folder of standalone \code{.shp} / \code{.gpkg} / \code{.geojson} files.}

\item{output_dir}{Directory for cleaned outputs and the \verb{checks/} subfolder.}

\item{layers}{Named character vector mapping admin levels to a layer name
(for a \code{.gdb} / \code{.gpkg}) or a filename stem (for a folder of files). Names
must be a subset of \code{adm0} / \code{adm1} / \code{adm2}; only the levels present are
processed, so pass a single entry to clean one level. Matching is
case-insensitive, preferring an exact match and otherwise the substring
match. Defaults to all three WHO \verb{GLOBAL_ADM*} layers.}

\item{transform}{Whether to reproject each layer to \code{crs}. The reprojection
is skipped automatically when a layer's CRS is already equivalent to \code{crs}
(ignoring cosmetic CRS-label differences), so no coordinates are swept
needlessly. Default \code{TRUE}.}

\item{crs}{Target CRS as an EPSG code. Default \code{4326} (WGS84).}

\item{fix_issues}{Whether to repair geometries (in addition to flagging
them): drop Z/M dimensions, make invalid geometries valid
(\code{\link[sf:valid]{sf::st_make_valid()}}), and strip sliver holes and sliver polygon parts
smaller than \code{sliver_area}. Real enclaves, lakes and islands above the
threshold are kept, and a feature never loses its largest part. Set \code{FALSE}
for the fastest run (flag-only). Default \code{TRUE}.}

\item{sliver_area}{Area threshold in square metres below which interior holes
and detached polygon parts are treated as digitising artifacts and removed
when \code{fix_issues = TRUE}. Default \code{1e4} (1 hectare).}

\item{output_format}{Serialization format for the cleaned shapes, \code{"rds"} or
\code{"qs2"}. \code{"qs2"} writes and reads large geometries far faster (needs the
qs2 package). Default \code{"rds"}.}

\item{verbose}{Whether to print a cli progress summary. Default \code{TRUE}.}
}
\value{
\code{output_dir}, invisibly. The function's outputs are the files it
writes (\code{{ext}} is \code{output_format}):
\itemize{
\item \verb{spatial_global_\{level\}.\{ext\}}: cleaned per-level shapes;
\item \verb{spatial_\{level\}_long_shape.\{ext\}}: year-expanded shapes (ADM1, ADM2);
\item \verb{checks/spatial_*_\{level\}_*.csv}: any validity/duplicate issues.
}
}
\description{
Reads the country (ADM0), province (ADM1) and district (ADM2) boundary layers
from any source \link[sf:st_read]{sf} can open and turns each into a cleaned,
validity-checked, reprojected \code{sf} object written to \code{output_dir}. For ADM1
and ADM2 a year-expanded "long" shape (one row per active year) is also
written, ready for temporal point-in-polygon joins.
}
\details{
\code{input_path} may be any of:
\itemize{
\item an Esri geodatabase (\code{.gdb}) or GeoPackage (\code{.gpkg}) holding the
three admin layers, matched by name via \code{layers};
\item a folder of standalone files -- shapefiles (\code{.shp}), GeoPackages
(\code{.gpkg}) or GeoJSON (\code{.geojson} / \code{.json}) -- one per level, matched by
filename via \code{layers}.
}

Per layer the cleaner:
\itemize{
\item reads the layer and snake_cases its columns (via janitor);
\item parses \code{startdate} / \code{enddate} to \link[lubridate:date_utils]{lubridate::Date} and derives \code{year_start} /
\code{year_end};
\item normalises the Cote d'Ivoire country name;
\item renames the admin name columns to the package's canonical \code{adm0} /
\code{adm1} / \code{adm2}, the level GUID column to \code{adm0_guid} / \code{adm1_guid} /
\code{adm2_guid}, and the geometry column to \code{shape};
\item runs validity, empty-geometry and duplicate (GUID and name) checks,
writing a CSV per issue found into a \verb{checks/} subfolder;
\item when \code{fix_issues = TRUE}, repairs geometries: drops Z/M, makes
invalid geometries valid, and removes sliver holes and sliver polygon
parts below \code{sliver_area};
\item reprojects to \code{crs} when \code{transform} is \code{TRUE}.
}
}
\examples{
\dontrun{
# an Esri geodatabase with the GLOBAL_ADM* layers
process_spatial("path/to/who.gdb", output_dir = "outputs/spatial")

# a folder of shapefiles named adm0.shp / adm1.shp / adm2.shp
process_spatial(
  "path/to/shapefiles",
  output_dir = "outputs/spatial",
  layers = c(adm0 = "adm0", adm1 = "adm1", adm2 = "adm2")
)
}

}
