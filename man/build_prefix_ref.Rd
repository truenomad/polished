% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{build_prefix_ref}
\alias{build_prefix_ref}
\title{Build a (prefix, year) -> unique admin-value reference}
\usage{
build_prefix_ref(
  data,
  admin_col,
  epid_var = "epid",
  year_var = "year",
  prefix_length = 11
)
}
\arguments{
\item{data}{Data frame containing \code{epid_var}, \code{year_var}, and \code{admin_col}.}

\item{admin_col}{Name of the admin column to summarise.}

\item{epid_var}{EPID column name. Default \code{"epid"}.}

\item{year_var}{Year column name. Default \code{"year"}.}

\item{prefix_length}{Prefix length passed to \code{\link[=epid_prefix]{epid_prefix()}}. Default \code{11}.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{prefix}, \code{year},
\code{n_candidates}, and \code{admin_col}.
}
\description{
For each geographic prefix and year, returns the admin value only when it
is unambiguous (exactly one distinct non-blank value); otherwise the value
is \code{NA} and \code{n_candidates} records how many distinct values competed.
}
\examples{
cases <- tibble::tibble(
  epid = c("NIE-BOS-AAA-1", "NIE-BOS-AAA-2", "NIE-BOS-BBB-1"),
  year = c(2024, 2024, 2024),
  district = c("BOSSO", "BOSSO", "BIRNI")
)
build_prefix_ref(cases, "district", prefix_length = 7)
}
