% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_afp.R
\name{clean_afp}
\alias{clean_afp}
\title{Clean POLIS AFP case data}
\usage{
clean_afp(
  data,
  cfg = polis_config(),
  shape = NULL,
  impute_geo = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A raw POLIS case data frame.}

\item{cfg}{A \code{\link[=polis_config]{polis_config()}} object (default \code{polis_config()}). Supply
\code{cfg$synonyms} to remap merged EPIDs and \code{cfg$qa} to route ambiguity flags.}

\item{shape}{Optional district shape that drives admin recovery. Either form
works and a single input does everything:
\itemize{
\item a polygon layer (\code{spatial_global_adm2}, an \code{sf} object) -- its long
form is derived here (as \code{\link[=process_spatial]{process_spatial()}} does) for the GUID/name
reconcile via \code{\link[=reconcile_admin_guids]{reconcile_admin_guids()}}, and its geometry drives
coordinate recovery via \code{\link[=impute_geo_from_coords]{impute_geo_from_coords()}} for cases still
missing a district but carrying coordinates;
\item a long ADM2 attribute table (\code{spatial_adm2_long_shape}) -- reconcile
only, since it has no geometry for the coordinate step.
}
Reconciliation adds a \code{geo_source} column. Default \code{NULL} (no shape-based
recovery).}

\item{impute_geo}{If \code{TRUE} (default) cases still missing \code{adm1}/\code{adm2} (and
their GUIDs) after reconciliation have them recovered from the EPID prefix
via \code{\link[=impute_geo_from_epid]{impute_geo_from_epid()}} (self-reference + prefix matching, every row
kept). Adds \verb{*_source} provenance columns.}

\item{verbose}{Emit cli progress messages for each phase. Default \code{TRUE}.}
}
\value{
A tibble of cleaned AFP records, one row per POLIS \code{id}, with columns
ordered id -> location -> time -> other. The canonical and derived columns
(\code{year_onset}, \code{month_onset}, \code{age_months}, the \verb{*_to_*} intervals,
\code{onset_date_quality}, \code{timeliness} and the 60-day follow-up flags) are added
only when their prerequisite source columns are present in \code{data}, so a
trimmed input yields a correspondingly trimmed output rather than an error.
}
\description{
Standardises one raw POLIS case table and derives the case-level analytic
variables AFP surveillance relies on:
\itemize{
\item canonical snake_case names (via the crosswalk + janitor) and
merged-EPID remapping;
\item parsed epidemiological/laboratory \verb{*_date} columns;
\item \code{year_onset} / \code{month_onset} from \code{paralysis_onset_date} (falling
back to the stool-1 then notification year when onset is missing), and a
numeric \code{age_months};
\item the standard onset-relative date intervals (\code{onset_to_notify},
\code{onset_to_invest}, \code{onset_to_stool1}, \code{onset_to_stool2},
\code{invest_to_stool1}, \code{stool1_to_stool2}, \code{notify_to_invest},
\code{onset_to_followup}), in days;
\item the \code{stool1_missing} / \code{stool2_missing} / \code{stool_missing} flags;
\item stool \code{timeliness} and the 60-day follow-up flags
(\code{needs_60day_followup}, \code{got_60day_followup}, \code{followup_on_time});
\item the fused analytic classification \code{classification_all} (with its
building blocks \code{vtype} / \code{vtype_fixed}) plus the Sabin flags
\code{sabin1} / \code{sabin2} / \code{sabin3} and a recomputed \code{hot_case};
\item normalised admin names and one row per POLIS \code{id} (latest by
\code{last_update_date}).
}
The raw POLIS \code{classification}, \code{polio_virus_types}, \code{vdpv_classifications},
\code{adequate_stool} and \code{paralysis_hot_case} fields are kept as-is alongside the
derived columns. The business key \code{epid} + \code{adm0} is asserted as a tripwire:
violations are flagged to QA, never dropped.
}
\examples{
raw <- data.frame(
  Id = c(1, 1, 2),
  Epid = c("A-1", "A-1", "B-2"),
  LastUpdateDate = c("2024-01-01", "2024-03-01", "2024-02-01"),
  ParalysisOnsetDate = c("2024-01-02", "2024-01-02", "2024-02-03"),
  NotificationDate = c("2024-01-05", "2024-01-05", "2024-02-06"),
  Admin0Name = c("NIGERIA", "NIGERIA", "CHAD"),
  check.names = FALSE
)
clean_afp(raw)

}
