% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{collapse_business_key}
\alias{collapse_business_key}
\title{Collapse business-key duplicates, keeping the latest record}
\usage{
collapse_business_key(data, key, date = "last_update_date", verbose = TRUE)
}
\arguments{
\item{data}{A data frame (already deduped by \code{id}).}

\item{key}{Character vector naming the business key columns.}

\item{date}{Name of the recency column (default \code{"last_update_date"}).}

\item{verbose}{Emit a cli summary of how many rows collapsed. Default \code{TRUE}.}
}
\value{
\code{data} with at most one row per non-blank \code{key} combination; rows
whose key is missing or blank are passed through unchanged. Row order is
not guaranteed.
}
\description{
After \code{\link[=polis_upsert]{polis_upsert()}} has reduced the data to one row per \code{id}, distinct ids
can still share a business key -- the same case re-entered under a new POLIS
Id. This keeps one row per \code{key} combination, the latest by \code{date}, so those
duplicates collapse. Rows with a missing or blank key value are passed through
untouched, never merged together.
}
\examples{
df <- data.frame(
  id = c(1, 2, 3),
  epid = c("A-1", "A-1", "B-2"),
  adm0 = c("X", "X", "X"),
  last_update_date = as.Date(c("2024-01-01", "2024-03-01", "2024-02-01"))
)
collapse_business_key(df, key = c("epid", "adm0"))

}
