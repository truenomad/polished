% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{remap_synonyms}
\alias{remap_synonyms}
\title{Remap merged EPIDs to their canonical value}
\usage{
remap_synonyms(data, synonyms = NULL)
}
\arguments{
\item{data}{A data frame with an \code{epid} column.}

\item{synonyms}{A data frame with columns \code{epid} (old) and \code{canonical_epid},
or \code{NULL} (default no-op).}
}
\value{
\code{data} with \code{epid} remapped where a synonym exists.
}
\description{
Rewrites the \code{epid} column using a synonym table (old EPID -> canonical EPID)
so that records POLIS has merged collapse together in the subsequent
\code{\link[=polis_upsert]{polis_upsert()}} step. A no-op when \code{synonyms} is \code{NULL}, so cleaners can call
it unconditionally.
}
