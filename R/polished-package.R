#' polished: streamlined downloader and preprocessor for WHO POLIS data
#'
#' `polished` pulls raw tables from the WHO POLIS OData API and turns them
#' into analysis-ready files for polio surveillance workflows. The package
#' is rebuilt incrementally; each module is added and verified on its own.
#'
#' @keywords internal
"_PACKAGE"
